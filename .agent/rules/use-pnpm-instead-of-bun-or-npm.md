---
trigger: always_on
---

always use pnpm instead of bun or npm for any other use other than expliscit needs. 

to install , update and add packages , use pnpm , 

to run one-off scripts to test something or something else, that does not change the package.json , use bun because it's better . 


